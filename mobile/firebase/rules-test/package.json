{
  "name": "readinglog-rules-test",
  "private": true,
  "description": "Firestore 보안 규칙 에뮬레이터 테스트 (Design §11.2)",
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --config ../../firebase.json --only firestore --project demo-readinglog \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.19.0"
  }
}
